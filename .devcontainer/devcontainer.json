{
	"build": {
		"dockerfile": "Dockerfile"
	},
	"features": {
		"docker-in-docker": {}
	},
	"initializeCommand": "git submodule update --init --recursive",
	"name": "ferronn-dev/ferronnizer",
	"postCreateCommand": "pre-commit install && pre-commit run -a"
}
