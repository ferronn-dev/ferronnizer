---
name: build
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v2
    - name: Run addonmaker
      uses: ferronn-dev/addonmaker@main
